agent-framework>=1.4.0
agent-framework-foundry-hosting
azure-identity>=1.17.0
httpx>=0.27.0,<1.0
# OpenTelemetry pinned to the exact versions hard-required by the transitive
# chain agent-framework-foundry-hosting -> azure-ai-agentserver-core ->
# microsoft-opentelemetry (opentelemetry==1.40 / instrumentation==0.61b0).
# Loose lower bounds + --pre made pip backtrack through dozens of versions and
# time out the remote build; exact pins collapse the resolution.
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp==1.40.0
opentelemetry-instrumentation-httpx==0.61b0
pydantic>=2.7.0
